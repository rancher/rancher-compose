api-server:
  service_schemas:
    kubernetesReplicationController:
      resource_fields:
        template:
          type: json
          create: true
    kubernetesService:
      resource_fields:
        template:
          type: json
          create: true
